// Ensure you import using PascalCase and correct file path

import Login from "@/components/atoms/login";

const page = () => {
  return (
    <div>
        <Login/>
    </div>
  );
};

export default page;
